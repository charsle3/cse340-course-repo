<%- include('partials/header') %>
<main>
    <h1><%= title %></h1>
    <h2>These are the categories of our service projects:</h2>
    
    <ul>
        
        <% categories.forEach(category => { %>
            <li><%= category.name %></li>
        <% }); %>

    </ul>
</main>
<%- include('partials/footer') %>